{
  "name": "jsonresume-theme-resumegenie",
  "version": "0.0.1",
  "description": "Simple theme for JSON Resume. Based on macchiato.",
  "author": "Russell Groves",
  "scripts": {
    "prepublishOnly": "npm run pretty && npm run lint && npm run test",
    "pretty": "prettier --write --ignore-path='*lock.json' '{src/**/*.{js,ts,json},./*{js,ts,json}}' ",
    "lint": "eslint *.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/GoodGuyGroves/jsonresume-theme-resumegenie"
  },
  "license": "MIT",
  "dependencies": {
    "address-format": "^0.0.3",
    "handlebars": "^4.7.6",
    "handlebars-wax": "^6.1.0",
    "moment": "^2.24.0"
  },
  "devDependencies": {
    "@commitlint/cli": "~8.2.0",
    "@commitlint/config-conventional": "~8.2.0",
    "eslint": "^6.5.1",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2",
    "prettier": "^1.18.2",
    "puppeteer": "^2.0.0"
  }
}
